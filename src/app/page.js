'use client';

import ChatInterface from '@/components/ChatInterface';

const Page = () => {
    return (
        <div className="min-h-screen">
            <ChatInterface />
        </div>
    );
};

export default Page;
